# Test Application

## Prerequisites
Requires nodejs and npm, may require sudo dependent on you setup.
``` bash
	npm install -g grunt
	npm install -g bower
	npm install -g forever
```

## Build application
``` bash
	npm install
	grunt build
```